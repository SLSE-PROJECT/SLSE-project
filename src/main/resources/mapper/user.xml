<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.codenova.slseproject.repository.UserRepository">
    <insert id="create">
        insert into users(email, password, nickname, provider, provider_id, created_at, SLSE)
        values (#{email}, #{password}, #{nickname}, #{provider}, #{providerId}, now(), #{SLSE})
    </insert>
    <select id="selectByEmail" resultType="user">
        select * from user where email = #{email}
    </select>
    <select id="selectByProviderAndProviderId" resultType="user">
        select * from user where provider = #{provider} and provider_id = #{providerId}
    </select>
</mapper>